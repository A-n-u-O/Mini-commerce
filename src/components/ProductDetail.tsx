// import { getProductBySlug } from "@/app/lib/api";
// import { Product } from "@/app/lib/types";

// export default function name({product}:{product.slug: string}) {
//     const productDetail = getProductBySlug((product: { slug: string; })=> product.slug)
//     return<div>slug</div>
